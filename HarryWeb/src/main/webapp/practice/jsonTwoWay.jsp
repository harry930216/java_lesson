<%--
  ════════════════════════════════════════════════════════════
   jsonTwoWay.jsp — 後端:解析 JSON 再回 JSON(雙向)
   對照老師:brad24.jsp
   ──────────────────────────────────────────────────────────
   目標:收到 JSON 字串 → 解析出裡面的欄位 → 運算 → 再以 JSON 回傳。
   驗收:四則運算都對,前後端都用 JSON。
   卡住:先分辨卡在「觀念」還是「語法」,再回來問。
  ════════════════════════════════════════════════════════════
--%>

<%-- ↓ 從這裡開始,自己從零打 --%>
